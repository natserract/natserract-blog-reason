
module Styles = {
    open Css;
    
    let container = style([
        margin2(`px(0), `auto),
        maxWidth(`em(41.0)),
        width(`percent(92.0)),
        padding2(`px(10), `px(0))
    ]);
}